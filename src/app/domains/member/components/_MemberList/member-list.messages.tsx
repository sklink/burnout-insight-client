import { defineMessages } from 'react-intl';

export const scope = 'app.components.ManageMembers';

export default defineMessages({
  buttonRevokeAccess: {
    id: `${scope}.buttonRevokeAccess`,
    defaultMessage: 'Revoke Access',
  },
});
